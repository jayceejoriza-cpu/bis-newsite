# Dark Mode Timing Fix - TODO

## Steps

- [x] Plan approved
- [x] Create `assets/js/dark-mode-init.js` — IIFE to prevent FOUC
- [x] Update `assets/css/style.css` — Add `html.dark-mode` + `.no-transition` rules
- [x] Update `assets/js/script.js` — Remove duplicate dark mode load logic
- [x] Update `index.php` — Add init script in `<head>`
- [x] Update `residents.php` — Add init script in `<head>`
- [x] Update `households.php` — Add init script in `<head>`
- [x] Update `certificates.php` — Add init script in `<head>`
- [x] Update `requests.php` — Add init script in `<head>`
- [x] Update `blotter.php` — Replace inline IIFE with init script
- [x] Update `officials.php` — Add init script in `<head>`
- [x] Update `reports.php` — Replace inline IIFE with init script
- [x] Update `activity-logs.php` — Add init script in `<head>`
- [x] Update `archive.php` — Add init script in `<head>`
- [x] Update `backup.php` — Add init script in `<head>`
- [x] Update `barangay-info.php` — Add init script in `<head>`
- [x] Update `restore.php` — Add init script in `<head>`
- [x] Update `official-user.php` — Add init script in `<head>`
- [x] Update `roles.php` — Add init script in `<head>`
- [x] Update `user-profile.php` — Add init script in `<head>`
- [x] Update `login.php` — Add init script in `<head>`, clean up bottom script

## ✅ All tasks complete
